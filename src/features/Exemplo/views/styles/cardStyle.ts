const textoPadrao = {
  fontFamily: 'Lato'
};

const styles = (theme: Object) => ({
  root: {
    flexGrow: 1
  },
  paper: {
    margin: 'auto',
    maxWidth: 500
  },
  image: {
    width: 128,
    height: 128
  },
  img: {
    margin: 'auto',
    display: 'block',
    maxWidth: '100%',
    maxHeight: '100%'
  }
});

export default styles;
